// JavaScript Document

// Scripts written by YOURNAME @ YOURCOMPANY

// init layzr
const layzr = Layzr();

document.addEventListener("DOMContentLoaded", event => {
  layzr
    .update()
    .check()
    .handlers(true);
});
